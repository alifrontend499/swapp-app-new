import 'package:flutter/material.dart';

// MAIN COLORS
const appPrimaryColor = Color.fromRGBO(241, 215, 146, 1);

// BUTTONS COLORS
// common colors
const btnColorFacebook = Color.fromRGBO(72, 103, 170, 1);
const btnColorGoogle = Color.fromRGBO(66, 134, 245, 1);
const btnColorApple = Color.fromRGBO(13, 13, 12, 1);

// primary btn
const appPrimaryBtnColor = appPrimaryColor;
const appPrimaryBtnColorOnPrimary = Colors.black;

// white btn
const appWhiteBtnColor = Colors.white;
const appWhiteBtnColorOnPrimary = Colors.black;

// gray btn
const appGrayBtnColor = Color.fromRGBO(229, 229, 229, 1);
const appGrayBtnColorOnPrimary = Colors.black87;

// black btn
const appBlackBtnColor = Colors.black;
const appBlackBtnColorOnPrimary = Colors.white;

// TEXT COLORS
const appTextColorPrimary = Color.fromRGBO(76, 163, 248, 1);

// BORDER COLORS
const appBorderColorPrimary = Color.fromRGBO(76, 163, 248, 1);

// PROGRESS BAR COLORS
const appProgressBarType1BgColor = Color.fromRGBO(229, 229, 229, 1);
const appProgressBarType1BarColor = Color.fromRGBO(156, 157, 149, 1);

// background colors
const appGreyDarkBGColor = Color.fromRGBO(229, 229, 229, 1);
const appGreyHighlightBGColor = Color.fromRGBO(229, 229, 229, 1);
const appGreyLightestBGColor = Color.fromRGBO(237, 237, 237, 1);